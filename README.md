A very basic Bottle application to demonstrate a HTTP endpoint for a database query for FMB.

`DATABASE_URL` format:

    oracle+cx_oracle://username:password@hostname.domain:port/?service_name=service.name
